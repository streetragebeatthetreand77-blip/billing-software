rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated admins can read and write transaction, customer, or inventory logs
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
